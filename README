Este pacote possui o compilador de uma linguagem simples, baseada em C:

Lex:
    Para rodar o teste do lexer, basta utilizar o comando make:

    $ make clean
    $ make 

    deve deletar arquivos antigos e compilar o projeto, rodando os testes em cima
    do arquivo ./languagefiles/ex.czar 

    Este arquivo possui cadeias de caractéres que seriam reconhecidas pelo lexer.

    O  código do Lexer se encontra disponível na pasta ./lex/ 
